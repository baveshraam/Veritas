# Node / Next.js (apps/web)
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo

# Python (apps/api, packages/*, data/)
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local Catalyst / Zoho CLI config & debug logs (machine-specific)
.catalystrc
catalyst-debug.log

# Local Claude Code tooling (personal, not project-shared)
.claude/
.agents/
CLAUDE.local.md

# Local Veritas tooling state
.veritas/

# Skills lock file (auto-generated tooling state)
skills-lock.json

# Build / export output (Next.js static export)
client/

# Zip archives (data exports, build artifacts)
*.zip

.vercel
.env*
Veritas-QUICKML-Production
self_client.json

.build.log

# transient deploy artefacts
.objkey
.appsail-deploy.log

.playwright-mcp/

# Docs stay local only — never committed. Only the root README.md is tracked.
docs/
*.md
!/README.md
